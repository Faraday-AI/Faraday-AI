<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
        }
        .alert-header {
            background-color: {% if severity == 'critical' %}#ff4444{% elif severity == 'warning' %}#ffbb33{% else %}#00C851{% endif %};
            color: white;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .details {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .recommendations {
            background-color: #e3f2fd;
            padding: 15px;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="alert-header">
        <h2>Alert: {{ message }}</h2>
        <p><strong>Severity:</strong> {{ severity }}</p>
        <p><strong>Type:</strong> {{ alert_type }}</p>
        <p><strong>Time:</strong> {{ timestamp }}</p>
    </div>

    <div class="details">
        <h3>Details:</h3>
        <ul>
        {% for key, value in details.items() %}
            <li><strong>{{ key }}:</strong> {{ value }}</li>
        {% endfor %}
        </ul>
    </div>

    {% if recommendations %}
    <div class="recommendations">
        <h3>Recommendations:</h3>
        <ul>
        {% for rec in recommendations %}
            <li>{{ rec }}</li>
        {% endfor %}
        </ul>
    </div>
    {% endif %}
</body>
</html> 